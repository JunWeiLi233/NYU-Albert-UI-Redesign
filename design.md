# Better Albert Design

## Source of truth

- Status: Active — full-page redesign contract
- Last refreshed: July 27, 2026
- Primary product surfaces: the authenticated Albert shell; its selected Home, Academics, Grades & Transcripts, Finances, and Personal Info workspaces; recognized deep pages; exact Class Search; and the native Other Resources navigation overlay.
- Evidence reviewed: this file's NYU website evidence appendix, the current NYU Students page and its Key Links on July 26, 2026, `nyu-albert-extension-plan.md`, the extension source and sanitized fixtures, and a privacy-preserving authenticated walkthrough of Albert's primary and deep hub navigation on July 16, 2026.

## Brand

- Personality: institutional, clear, calm, editorial, trustworthy, and functional.
- Trust signals: label the extension as an unofficial local enhancement, keep native Albert visible, expose a clear but tertiary “Original Albert” escape hatch with the accessible action name “Use original Albert,” and never imply that extension-rendered content is authoritative. The escape hatch must remain a 44px target without visually competing with Find a task or Start here.
- Avoid: generic SaaS chrome, marketing-page layouts, gradients, glass effects, excessive rounding or shadows, decorative motion, and unofficial recreations of NYU marks.

## Product goals

- Goals: make the full authenticated Albert experience feel coherent and NYU-aligned, improve visual hierarchy across every recognized page family, survive PeopleSoft partial navigation and frame rendering, and preserve immediate access to the native interface.
- Non-goals: changing authentication; automating login, MFA, enrollment, payment, drop, or swap actions; replacing official data or controls; calling undocumented APIs; reconstructing student records inside extension UI.
- Success signals: one isolated full-height application frame appears in the top portal document, every recognized Albert document receives exactly one reversible structural adapter, each selected portal workspace has a distinct content hierarchy, Class Search has a proven filter/results workspace, the non-document Other Resources overlay remains delegated to Albert, a separate NYU resources group exposes only uniquely verified native overlay destinations across every area, page context follows partial navigation, students at every supported width can search or scan what they need to do before learning Albert's area names, direct task shortcuts already present in the current native view remain discoverable across family boundaries, native skip navigation never crosses extension task cards unless it has keyboard focus, disablement removes every extension-owned marker and style, failures leave native Albert usable, and only interface preferences are stored.

## Binding definition of full-page redesign

“Full-page redesign” means every recognized Albert document receives an extension-owned application frame and a structure-aware content layout covering its page header, primary sections, navigation or tool areas, lists, tables, forms, alerts, empty/error states, and supported read-only dialogs. A font or color swap, a title border, a generic white container, or a header alone does not satisfy this requirement.

The redesign operates on original native DOM in place. Adapters may annotate stable structural containers and apply scoped layout, but they must not clone or reconstruct student data, synthesize destinations, replace native controls, change transaction semantics, move controls between forms, or visually reorder focusable controls away from their DOM order.

## Personas and jobs

- Primary personas: NYU students using Albert in a Chromium browser, with first-time students as the primary wayfinding and terminology stress case.
- User jobs: understand where they are, discover common tasks without already knowing PeopleSoft terminology, move among Albert's primary areas, scan dense native pages more comfortably, keep using official Albert controls, and disable the enhancement instantly when desired.
- Key contexts of use: authenticated, data-sensitive academic workflows where clarity and native-system trust matter more than decoration.

## Information architecture

- Primary course search: when the exact native Course Search control is uniquely verified on Home, Find Classes remains the first one-step Start here action and receives the only filled primary-action treatment in that shortcut group at every supported width. Outside Home, the header exposes a separate visible Find classes shortcut: it delegates directly to that exact original Course Search control when the control remains uniquely available in the trusted native-control document; otherwise it carries the same one-step intent through the exact native Home navigation and opens that original Course Search as soon as Home is ready, with the visible explanation “Open Course Search.” The shortcut is omitted on Home to avoid duplicating Start here, while Other Resources is open to preserve resource-only search, and whenever neither a verified Course Search control nor Home destination exists. The corresponding Find classes starter receives the same emphasis in the general task finder. Albert currently routes that exact control through its supported Enrollment Shopping Cart component before the actual Class Search form; for a student-originated Better Albert Find Classes activation only, an ephemeral intent remains in the current portal document for at most ten seconds and may relay through Albert’s existing same-origin portal frames to the exact allowlisted child component. That component consumes the intent only after it uniquely verifies and activates its single visible, enabled, already-selected native Class Search mode and single exact native Search control; an early message received before PeopleSoft finishes rendering remains unconsumed so a bounded retry can succeed. This removes Albert’s otherwise unexplained second launcher click without constructing a URL, supplying criteria, submitting a course query, adding a class, or persisting intent. Missing, expired, cross-document, non-portal, non-cart, unchecked, hidden, disabled, duplicate, or structurally ambiguous evidence fails open to Albert’s unchanged two-step path. Direct activation disappears when the original Course Search control is missing, duplicate, detached, hidden, or disabled.
- Course-search cue: the closed finder launcher explicitly says “Find classes, tasks, and NYU resources,” and the opened field is labelled “Search classes, tasks, and resources,” making class discovery visible before and after the panel opens instead of relying on the student to infer that courses count as tasks. The empty-search placeholder then leads with “find a course,” teaches the safe “new student” resource path, and supports the same exact phrase that resolves to the uniquely verified native Course Search control with Enter for one-step activation. Student-language requests such as “course catalog,” “class catalog,” and “browse course offerings” use that same exact control as conservative aliases, while broad phrases such as “what classes do I need” remain available for degree-requirement matching. When that exact control is unavailable in the current Albert view, the same phrase carries a one-step intent through the verified native Home area and opens its Course Search when ready, instead of pointing back to the already-open Academics area or inventing a Class Search URL. This preserves a one-step handoff whenever Albert exposes Course Search, including cross-workspace navigation. The cue remains guidance only; it never interprets course names, queries a catalog, supplies criteria, or submits Albert’s search.
- Class Search entry focus: after Albert loads an exact supported Class Search document, initial keyboard focus moves once to the uniquely verified combined or subject search field; on selector-only classic pages it moves to the exact native Subject selector because Academic Career is normally preselected. This completes the one-action Find Classes handoff in a ready-to-type or ready-to-select state. Focus never moves when the control is missing, ambiguous, disabled, hidden, inert, or when the document already owns a more specific active element, and later DOM reconciliation never steals focus back from the student. Native Search, Add to Cart, Enroll, and Continue actions remain untouched.
- Class Search recovery: when one exact native `role="alert"` message uniquely describes one exact `aria-invalid="true"` search control, preserve the native message and ARIA relationship verbatim, promote that alert with the danger surface, mark the invalid native control with the danger border, and move initial focus once to that control so the next corrective step is immediate. Multiple, ambiguous, missing, disabled, hidden, or inert validation targets receive no promotion or focus. When a result region has no native result rows and contains exactly one native `role="status"` whose text explicitly identifies no classes, courses, or results, keep that native status text and neutral semantics verbatim, then add the reversible visible and accessible recovery cue “Adjust your search, then use Search again.” An existing native `aria-description` or `aria-describedby` always wins, and loading, validation, ambiguous, or non-empty status messages receive no empty-result treatment.
- One-action resource discovery: opening the exact native Other Resources directory automatically presents the resource-only Search NYU resources surface when at least one original anchor has been uniquely verified, matching the search-first expectation of nyu.edu-style service discovery without adding a destination. The original native directory remains open, connected, and visually paused underneath. Because the searchable view already lists every verified resource, its separate handoff is explicitly labelled “View Albert resource directory” rather than the misleading “Browse all”; that action reveals the unchanged native directory, while Close or Escape exits the complete resource experience in one action by delegating to Albert’s original Other Resources toggle and restores focus to the current resource opener after the native view settles. Closing search must never unexpectedly reveal a second navigation layer. When no native resource anchor can be verified, the extension does not open an empty search surface.
- Primary navigation: Home, Academics, Grades & Transcripts, Finances, Personal Info, and Other Resources. Each destination pairs the native area name with a concise task-oriented hint for students who do not yet know Albert's organization. Home leads with Find classes because its verified Course Search and Weekly Schedule controls own direct class discovery and schedule review. Academics instead describes “Course planning, advising, enrollment, and degree progress”; it must not promise direct class search or schedule access that is absent from that view. Accordingly, a task-finder request for “class schedule” falls back uniquely to the verified Home area when no exact native schedule shortcut is present, rather than returning both Home and Academics. Grades & Transcripts must lead with “View grades” before transcripts and enrollment proof so the area’s most common first-semester job is never hidden behind records terminology. Finances must name balance checking, tuition payment, bills, and financial aid rather than relying on generic “account” or “financial services” language. Student-outcome phrases such as “amount due,” “how much I owe,” and “review charges” may route to the verified Finances area when the exact native control is not in the current view; once there, the exact native balance or account control wins. Personal Info must distinguish reviewing official details from updating contact information rather than implying that every identity field is directly editable. When the exact edit link is not in the current view, student-language requests such as “new address,” “new phone number,” “new email address,” and “new emergency contact” route to the verified Personal Info area; on Personal Info, the exact native edit link wins. Need phrases such as “technology help,” “student support,” “printing,” and “libraries” may route to the verified Other Resources area when no matching native resource anchor is present in the current directory; an exact native resource always takes precedence. This fallback opens Albert’s real directory and never invents a destination. Common advising language including “advisor appointment” and “schedule advisor appointment” resolves to the exact native appointment control whenever Academics verifies it. Extension navigation delegates to matching native Albert links and never synthesizes destinations. Changing areas resets the extension rail to its workspace title and current page tools so a prior rail scroll position cannot hide the next student task. On recognized top-level family pages, the redundant native family strip is visually suppressed while its original controls remain connected as the delegation authority; it returns immediately on disablement or fail-open rollback, and remains visible in child, generic, and deep documents that do not own the shell.
- Personal Info search vocabulary: exact “address,” “date of birth,” “gender,” and “legal name” queries prefer their corresponding uniquely verified native controls. The verified “Indicate My Pronouns” destination also covers “name pronunciation” because it opens Albert’s native Pronouns & Name Pronunciation page; no identity value is copied or cached.
- Secondary tools: present allowlisted, currently present native destinations as descriptive Quick access links rather than unexplained system labels. On every recognized family page, verified page tasks appear immediately after Find a task and before the broader six-area directory at every supported width, so the first desktop viewport prioritizes the student’s current job instead of the site map. On Home, the exact native Course Search and Weekly Schedule destinations plus the uniquely verified native Holds and Enrollment Dates status cards form a “Start here” block in that position. Albert nests the selected-term schedule links inside its shopping-cart container on some releases; when exactly one schedule-link cluster or selected schedule block is verified, mark that native block with the reversible “Today and weekly schedule” cue while preserving its semester heading and controls. Find Classes remains a visible one-click action in the initial desktop viewport as well as on phones; Check Holds and the student-language “When Can I Register?” action focus and center their original status cards rather than merely reopening Home or copying student data. These native cards receive only reversible focus targets and region markers, and an ambiguous or missing card produces no shortcut. When Albert’s exact Other Resources trigger is also verified, the same block adds a one-step “NYU resources” action for calendars, offices, support, and campus services; it delegates to the original trigger and disappears while the authoritative native directory is open. Academics may expose Academic Planner, Degree Progress Report, What If Report, graduation status, and two deliberately distinct advising actions: “Find My Advisor” focuses and centers one uniquely verified native My Advising Network section without copying its private values, while “Schedule Advisor Meeting” delegates only to Albert’s exact appointment control. A missing or ambiguous advising section produces no discovery shortcut. Records may expose the distinct National Student Clearinghouse and MyHub enrollment-verification destinations, Test Scores, unofficial transcripts, and transfer credit. When Records also verifies the exact original University Registrar resource, the same Quick access group adds “Official Transcript Guidance” with the visibly attached explanation that it opens NYU Registrar ordering instructions; both the label and explanation must remain visible in compact layouts so the control cannot be mistaken for an order submission. Finances may expose the bursar balance, bursar account, statement, and financial-aid status; its visible descriptions must lead with student outcomes such as what is owed, paying tuition, and reviewing charges before naming Bursar or eSuite, and ordinary “amount due” or “review charges” searches must resolve directly to the exact verified native control instead of stopping at the broader Finances area. Personal Info may expose demographic details and exact native edit entry points for addresses, phones, email addresses, and emergency contacts. Below 900px these task controls wrap into visible rows instead of becoming an unannounced horizontal strip, use NYU-violet text and borders against white so the one-step actions remain visually distinct from surrounding navigation, and allow long labels to wrap within their 44px-minimum targets so no verified action begins off-screen. When a compact shortcut’s native label names a provider, system, report, or guidance destination rather than making the student outcome self-evident—currently Clearinghouse and MyHub enrollment records, official-transcript guidance, Bursar/eSuite/statement destinations, financial-aid status, and What If Report—the concise description remains visibly attached to that control. When every connected, enabled, natively visible control in a page-specific PeopleSoft directory maps uniquely to Quick access, that repeated directory remains connected as the delegation authority but becomes inert and visually collapsed; any unknown, ambiguous, or transaction-changing control keeps the complete native directory visible.
- Universal resources: keep Academic Calendar, University Registrar, Wellness Center, and Housing as a concise featured “NYU resources” group on every area. The task finder additionally exposes the complete 21-link allowlisted native directory—Academic Calendar, Course Feedback Results, FERPA, Replacement Diploma, University Registrar, NYU Brightspace, NYU Connect, NYU Summer, Wasserman, Financial Aid, NYU Card Center, OGS, Office of Global Programs, Law Housing, Campus Safety, Wellness Center, Housing, Student Services, Campus Resources, Academic Support, and Student Life—grouped as Academic & records, Learning & career, Money & campus services, Global opportunities, and Wellbeing & campus life. Every resource appears only when the exact native Other Resources submenu exists once and contains exactly one matching direct anchor. Hidden submenu state is expected and does not make a verified native link unavailable. Missing, duplicate, detached, disabled, or nested lookalike links are omitted. Activation delegates to the original anchor; the extension never copies or constructs its URL. These surfaces supplement rather than replace the native Other Resources overlay.
- Native resource directory: Albert keeps its single exact Other Resources submenu connected even while closed, so presence and removal of `hidden` are not sufficient open-state evidence. Mirror Albert’s verified toggle contract instead: explicit `hidden`, `aria-expanded`, or `aria-hidden` state wins when present; otherwise read the selected class named by the exact native `toggleMegaMenu` handler (currently `megaMenuSelected`). Only while that authoritative state is open, mark and present the same native list as a titled, full-height white directory surface beside the desktop rail or below the compact header. A closed-but-connected submenu must leave Home Start here, Find classes, ordinary task discovery, and the “Other Resources” label intact. A concise trust note explains that every entry retains its official NYU destination, verified links receive category overlines without moving or recreating them, and the visible finder launcher becomes “Search NYU resources.” At desktop widths, each category-start item spans the complete directory grid and pairs its category label with the original first link, creating five clear service-group leads instead of irregular column-one starts and empty grid cells; the same native anchors remain a simple one-column sequence on phones. Opening resource search above the directory enters a resource-only mode that searches all verified original anchors, omits unrelated Albert areas and task shortcuts, and delegates a selected result back to its exact native link. Before the student types, a compact “Popular resources” group offers Academic dates, Course materials, Financial aid, ID card, Health & counseling, Housing, Tech & Wi‑Fi, International students, Student success, Student support, and Campus safety only when the current verified directory contains the corresponding exact native anchor; choosing one delegates directly to that original anchor, and the group disappears as soon as the student types. Course materials delegates to the exact original NYU Brightspace anchor, reflecting the current NYU Students page’s Key Links hierarchy without adding or reconstructing a destination. Tech & Wi‑Fi delegates to the exact original Campus Resources anchor, reflecting NYU’s current Campus Resources and Student Tech Guide hierarchy for information technology, Wi‑Fi, printing, computer labs, libraries, maps, and transportation. Student success delegates to the exact original NYU Connect anchor, reflecting NYU’s current Student Success hierarchy and NYU Connect’s official roles for individualized support, Success Network access, support appointments, and assistance requests. Student support delegates to the exact original Student Services anchor, reflecting the public NYU Students page’s prominent Get Support hierarchy without claiming that the native destination is StudentLink or a student-success specialist; Career help remains available through search and the Learning & career category. Campus safety is a direct service label only; the extension makes no emergency-response or availability claim. The field says “Search by service or need” rather than repeating Albert’s system label, and supports ordinary verified needs such as course or class materials, a syllabus, general student support, needing help with NYU, student-success specialists, a Success Network, support appointments, getting involved, student clubs or organizations, campus events, financial aid, counseling, a counselor or doctor, an ID card, housing, technology help, Wi‑Fi, printing, libraries, maps, transportation, a career center, career help, campus police, or feeling unsafe without inventing a destination. Student-success, Success Network, support-appointment, and request-assistance language delegates only to the exact verified NYU Connect anchor. Getting involved, club, organization, and campus-event language delegates only to the exact verified Student Life anchor, matching the current NYU Students Getting Involved vocabulary without copying its public destinations. Those phrases remain conservative aliases for the exact verified NYU Brightspace, NYU Connect, Student Life, Student Services, Campus Resources, Wellness Center, Wasserman, and Campus Safety anchors; they never add a new URL, service-status claim, appointment-availability claim, or emergency claim. While this modal search is open, the original directory remains connected and open as the authority but its duplicate list, title, pointer input, and scrolling pause visually behind the search surface. “View Albert resource directory” closes only the modal and restores that original directory unchanged; Close and Escape instead delegate to the original Other Resources toggle and return directly to the current Albert page. Home additionally exposes the verified native trigger as a visible “NYU resources” Start here action so the directory is reachable without rail scrolling or prior knowledge of Albert terminology. Its stacking layer must remain above non-modal Albert alerts, carousels, and promotional content so every link stays visible. The shell mirrors the authoritative native open state with `aria-expanded`; while the browsable directory is open, the original trigger is labelled “Close Other Resources” and remains its authoritative dismissal path because Albert does not close the directory on Escape. On compact widths, other area controls temporarily collapse so this 44px-minimum Close action stays visibly pinned in the header. Page-family Quick access and the duplicate featured-resource strip are temporarily removed while this authoritative directory is open, restoring on close so compact screens dedicate their limited space to the selected resource task. The general task finder closes before delegation, and real native modal lifecycle rules remain authoritative.
- Task discovery: an on-demand “Find a task” panel repeats only the currently verified areas, every unique visible enabled allowlisted page tool already present in a verified native container, and universal NYU resource links; it adds no URLs or availability inference. Its launcher sits immediately after current-workspace context and before area navigation, keeping the primary task path visible at ordinary desktop heights instead of below a long services rail. The closed launcher visibly says “Find classes, resources, and new-student help,” so first-week discovery is explicit before the panel opens. A visible `/` key hint and `aria-keyshortcuts="/"` expose the one-keystroke search path: pressing `/` from non-editable page content opens the available task or resource finder and focuses search, while native inputs, textareas, selects, contenteditable regions, and textbox widgets retain the keystroke unchanged. A local, ephemeral search field filters those verified destinations across student-job descriptions, native labels, and resource categories and keywords. Before the student types, a compact “Common tasks” group offers Find classes, Class schedule, Course materials, Academic dates, Housing, New student help, Check holds, When can I register?, To-do list, Meet advisor, View grades, Check balance, Pay tuition, and Financial aid status starters, but only when the current verified view model contains a matching destination. The sequence keeps daily class access, urgent Home status, records, finance, and first-week resource discovery together without growing the phone inventory. Course materials delegates to the exact verified native NYU Brightspace anchor, reflecting its current NYU Students Key Link prominence; Unofficial transcript remains fully available through direct search and Grades & Transcripts rather than occupying the first-semester starter set. Prefer these precise one-step starters over generic area launchers: Academic dates delegates to the exact verified Academic Calendar resource, while Check holds, When can I register?, and To-do list focus their exact verified native Home status cards. Transcript intent must remain exact: word-boundary matching prevents “official” from matching “unofficial”; an official-transcript request resolves only to the verified University Registrar resource, an unofficial-transcript request resolves only to Albert’s exact unofficial-record control, and an unspecified request such as “get my transcript” shows both verified choices instead of guessing. To-do list remains task-finder-only so the fixed desktop Start here rail retains every action inside an 800px viewport; enrollment proof, broader support, transcript retrieval, and personal-information actions remain available through search, visible area navigation, and the NYU resources directory. Choosing a starter opens its original native destination immediately when the phrase has exactly one verified result; if the phrase is ambiguous, it narrows the same local search and returns focus to the field instead of guessing. The general finder’s sticky jump bar stays task-oriented—Search, Areas, Task shortcuts, and one NYU resources jump—rather than repeating all five resource categories before students reach any result; the resource-only search keeps category-level jumps because its entire result set is that directory. Area results match their own name, description, navigation hint, and local student-language phrases; individual shortcuts match only their own label, description, and phrases, so an area-name query does not make every shortcut in that area appear relevant. Conversational framing such as “I want to,” “where can I find,” “where can I get,” or “I’m looking for” is reduced to its meaningful task words. In particular, “Where can I get tutoring?” resolves to the exact verified Academic Support anchor, reflecting NYU’s current Academic Services language around academic tutoring, University Learning Centers, and support resources; the extension still delegates only to Albert’s original link. Intent-bearing words such as “need” and temporal “when” remain in the strict first pass so “what classes do I need” can distinguish degree requirements from course search and “when can I register” can distinguish the native enrollment appointment from class discovery; only when that complete pass finds no verified destination does a second pass remove the support-language words “need” and “help,” allowing ordinary requests such as “I need to pay my tuition” to recover the existing verified Finances destination without weakening a successful precise match. When those meaningful words exactly name a verified generic resource, that resource suppresses specialized near-matches such as Law Housing. When any verified direct tool matches, it suppresses broader area results so the student gets the shortest safe path; when no direct tool is available in the current native view, student-language area hints still provide the safest verified next step instead of a false no-results state. Search receives focus when the finder opens, reports the visible result count, supports clearing without closing, resets whenever the finder closes, and shows a trust-preserving no-results explanation rather than inventing a destination. Every fresh open also resets the finder’s own scroll position to the search-first top while leaving the underlying Albert page position unchanged, so a student never resumes mid-inventory without the panel’s title or context. At zero results, that same clear control becomes the explicit one-step “Show all” recovery action, restores every currently verified task or NYU resource, and returns focus to search; it never broadens the verified source set. When a non-empty query has exactly one verified result, the field explicitly offers Enter as a one-step activation path and delegates to that result's original native area, tool, or resource control; Enter never chooses among multiple results. Area cards lead with the task-oriented job (“Bills, statements, and financial aid”) and identify the original destination second (“Finances · Albert area”). Direct tools are grouped by their Albert area but also lead with the student job (“Review aid status and requirements”) before the native tool name (“Financial Aid Status · Finances”). Accessible names include both concepts. Duplicate, hidden, disabled, detached, or out-of-boundary matches are omitted. Below 900px, task and resource search replace the viewport with one white, independently scrolling modal surface instead of inserting a partial-height panel above a second page scroll; native Albert scrolling pauses, search receives focus, and a visible top-level Close action remains in the first screen. Below 280px, where the heading is removed to preserve zoom reflow, the sticky section index retains the visible Close action. Closing restores the unchanged page position, compact area tabs, and native scrolling. The compact identity row keeps “Better Albert” on one line at phone widths. At 900px and above the finder’s white surface fills the workspace beside the fixed rail while its content stays bounded, following NYU's full “All NYU” navigation pattern without copying public-site destinations or branding assets.
- Mobile resource-first hierarchy: at phone widths up to 599px, resource-only search replaces the five category-level jump controls with one accessible Results jump so the index does not consume the first screen. Search and all 11 available 44px-minimum popular-resource starters remain intact in one independently scrollable row with a visible “Scroll for more” cue; the document itself must not overflow horizontally. The first verified resource destination must be fully visible inside an ordinary 400×800 viewport before scrolling, and the secondary “View Albert resource directory” escape moves after the verified result groups. At 600px and above the category-level jumps and wrapped popular-resource layout remain available. This ordering changes presentation only; the exact native directory toggle and anchors remain the delegation authority.
- Exact-result hierarchy: when a non-empty task or resource query resolves to one uniquely verified destination, present that result as one emphasized card aligned to the bounded search-row width on desktop and as the normal full-width single-column card on compact screens. Do not leave the only safe next step stranded in one half of a multi-column inventory. The prominence is presentation only: Enter and pointer activation still delegate to the same exact original native area, tool, or resource control, and the layout returns to the ordinary multi-result grid as soon as the query is cleared or has any other result count.
- Live Home status identity: Albert appends changing numeric counts to the visible To Do and Holds headings. Treat one trailing parenthesized integer as live status metadata rather than part of the card identity, so “Check Holds” and “To-do list” can focus their uniquely verified original cards. The match remains exact after removing only that suffix; duplicate cards, nonnumeric suffixes, missing headings, or headings outside the one verified Attention section produce no shortcut. The extension never copies, interprets, or persists the count or the card’s student-specific contents.
- Class Search result legibility: every adapter-verified native result row must contain its full course identity, meeting details, and long native description within the visible result-card boundary at every supported width. Fixed-width PeopleSoft wrappers and uninterrupted identifiers wrap inside the card instead of requiring a hidden horizontal pan or clipping text at a modal edge. The extension changes wrapping only; it must not truncate, rewrite, summarize, hide, or persist native course content or change the identity and behavior of native result links.
- First-semester vocabulary: cover registration changes, viewing grades, identity/profile updates, enrollment letters, tuition bills and dates, academic-calendar questions, finding an advisor, understanding what an advisor can help with, locating classrooms, reviewing current classes, and lost ID cards. Normalize ordinary articles and question framing before matching, so “a” and “an” never prevent an otherwise exact student task from resolving. On an adapted Academics page, “Who is my advisor?”, “find my advisor,” and advisor-contact questions resolve to the uniquely verified native My Advising Network section. If that section is unavailable or ambiguous, discovery intent falls back to the verified Academics area; it never becomes an appointment request. “What can my advisor help with?” remains broader Academics guidance because the native advising network verifies identity and contact context, not the scope of advising services. A true meeting or scheduling request may still use Albert’s exact verified advisor-appointment control when present. Current-class intent and catalog intent also remain distinct: “what classes am I taking?”, “show my classes,” and “what classes do I have today?” resolve only to the exact verified Weekly Schedule control, while “where is my classroom?” or an explicit classroom-location request resolves only to the exact verified Course Search control, matching NYU’s current guidance that Albert Course Search displays instruction mode and classroom location. On an adapted Home page, “course registration,” “find a course,” and classroom-location requests resolve to the exact native Course Search control; on Academics and other non-Home views, “class schedule,” “course registration,” “enroll in class,” and “register for classes” resolve in one step to the verified Home destination because that is where Albert owns the native schedule and Course Search controls. Enrollment-letter language resolves to the exact native Proof of Enrollment control when it is uniquely verified, otherwise to the verified Grades & Transcripts area; it never fabricates a letter or provider destination. Payment-plan language resolves to the exact native Pay Tuition & View Bills eSuite destination when it is uniquely verified; it never promises eligibility, rates, or plan availability. The public NYU Students labels “Tuition and Fees,” “Bills, Payments, and Refunds,” and “Manage Your Personal Finances” remain conservative aliases for the verified Finances area, never public URLs or synthetic financial actions. Neither path copies schedule or location data into the extension. Transaction language such as drop, swap, withdraw, or a name change resolves only to the safest verified Albert area unless a uniquely matched original native shortcut is already present. Exact enrollment-proof, calendar, and card-service phrases may use their verified original controls.
- Academic-date vocabulary: ordinary questions about when classes start or end, spring break, finals, final exams, registration deadlines, drop/add deadlines, withdrawal deadlines, pass/fail deadlines, Bursar deadlines, or commencement dates resolve only to the exact verified Academic Calendar resource. This follows NYU’s current Academic Calendar hierarchy—key dates and deadlines, holidays and breaks, school start/end dates, Registration Dates & Deadlines, and Bursar Deadlines—without copying dates, choosing a term, interpreting a deadline, or claiming that a date applies to the student’s school or program. Enter delegates directly to Albert’s original Academic Calendar anchor.
- Health-resource vocabulary: ordinary requests for mental-health counseling or services, a counselor, therapist, therapy, doctor, medical or counseling appointment, student health insurance, health requirements, immunization or vaccination records, pharmacy or prescription help, health records, same-day care, or urgent care resolve only to the exact verified Wellness Center resource. This mirrors NYU’s current Student Health Center hierarchy—medical and counseling services, appointments, insurance, health requirements, pharmacy, patient records, and immediate care—without diagnosing, triaging, collecting health information, promising availability, or reconstructing a clinical destination. Medical-emergency language that also matches Campus Safety remains visibly ambiguous rather than being guessed; Enter activates Wellness Center only when it is the single verified result.
- Housing-and-dining vocabulary: ordinary requests for on-campus housing, residence halls, housing applications or assignments, room changes, moving in, campus dining, dining halls, food on campus, or meal plans resolve only to the exact verified Housing resource. This follows NYU’s current Housing and Dining hierarchy, which groups residence halls, academic-year housing, applications and assignments, room changes, dining locations, and meal plans under one student-facing destination. The extension does not construct a housing or dining URL, open a housing application, choose a residence hall or meal plan, state availability, quote rates, or claim that a housing or dining policy applies to the student; Enter delegates only to Albert’s original Housing anchor when it is uniquely verified.
- First-week campus-service vocabulary: ordinary requests for NYU technology help, the IT Service Desk, Wi‑Fi, printing, computer labs, software, Duo or MFA, VPN, NetID access, NYU email, libraries, study spaces, campus maps, shuttles, or transportation resolve only to the exact verified Campus Resources anchor. This follows NYU’s current Campus Resources hierarchy—information technology, libraries, copy services, facilities, and transportation—and its Student Tech Guide’s explicit Wi‑Fi, printing, support, computer-lab, and new-student setup paths. The extension does not troubleshoot a device, collect credentials, reset an account, report service status, choose software, infer transportation schedules, or construct a deeper destination; Enter delegates only to Albert’s original Campus Resources anchor when it is uniquely verified.
- Accessibility-and-belonging vocabulary: general disability-service, academic- or testing-accommodation, accommodation-letter, assistive-technology, Accessibility Center, and Moses Center requests resolve only to the exact verified Campus Resources anchor because NYU’s current Campus Resources hierarchy includes the Moses Center for Accessibility and Inclusive Culture. Accessible-housing and housing-accommodation requests instead resolve to the exact verified Housing anchor, matching NYU’s Housing and Dining hierarchy and the Moses Center’s explicit housing-accommodation path. Commuter or off-campus, graduate, transfer, military or veteran, LGBTQ+, spiritual-life or faith-community, multicultural or students-of-color, and student-parent support requests resolve only to the exact verified Student Life anchor, following NYU’s current Communities and Groups hierarchy. The extension does not ask for a diagnosis, disability documentation, identity, religious belief, veteran status, family status, or accommodation details; it does not apply, assess eligibility, promise support, copy community destinations, or construct a deeper URL. Enter delegates only when the corresponding original Albert anchor is uniquely verified.
- Practical-campus vocabulary: ordinary requests for NYU bookstores or computer stores, buying textbooks, mail or package pickup, lockers, general facilities or maintenance, Fix It, repairs, gyms, fitness centers, or athletic facilities resolve only to the exact verified Campus Resources anchor. This follows NYU’s current Campus Resources hierarchy, which explicitly includes Bookstore and Computer Store, Mail Services, lockers and copying services, Facilities and Operations, 404 Fitness, and Palladium Athletic Facility. Housing-maintenance intent instead resolves to the exact verified Housing anchor because NYU’s current on-campus housing hierarchy exposes Maintenance Services within residential resources. Exact Campus Cash and Campus Cash refund searches resolve only to the verified NYU Card Center anchor; the extension makes no deposit, balance, refund-timing, or availability claim because NYU states that the Campus Cash program ended in 2025. The extension never buys an item, submits a repair, handles a package, checks a card balance, or constructs a deeper destination; Enter delegates only to the uniquely verified original anchor.
- Typo recovery: exact and strict natural-language results always win. Only after both strict and support-word-relaxed passes return no destination may the finder tolerate one insertion, deletion, or substitution in words of at least five characters. The recovered set remains limited to the same verified in-memory destinations; a typo that yields multiple matches stays visibly ambiguous and Enter does nothing. A one-word resource typo prefers the same-length resource label, so “housng” resolves to Housing rather than also surfacing Law Housing. Reverse-prefix matching is limited to ordinary one- or two-character suffixes, preserving useful inflections without collapsing a compound intent such as “classroom” into every destination containing “class.”
- Home priority: retain Albert's native source order and content identity, but give the To Do, Holds, and Enrollment Dates region more visual weight than the promotional/schedule region on desktop. Its native status panels use a responsive equal-card grid that collapses naturally on narrow screens; the extension never interprets, summarizes, or reprioritizes individual student records. The top-level Home page does not repeat Albert's 22-link cross-family PeopleSoft directory after the overview: all 18 task controls are represented in the verified task finder and its four resource links are represented by the verified Other Resources directory. The native directory remains connected as the delegation source but is inert and visually collapsed while the shell is active, then returns unchanged on disablement or fail-open rollback.
- Core routes/screens: recognized authenticated `/psp/` and `/psc/` documents on `https://sis.portal.nyu.edu/*`, plus the proven cross-origin Class Search/cart component at `https://sis.nyu.edu/psc/csprod/EMPLOYEE/SA/c/NYU_SR_FL.NYU_SSENRL_CART_FL.GBL`; launcher, authentication, and unrelated SIS components are excluded.
- Content hierarchy: identity and primary-navigation frame, page-family title and context, family-specific primary workspace, secondary native tools, supporting sections, then the original native transaction controls and messages.

## Design principles

- Progressive enhancement: extension UI must be additive, isolated, removable, and non-blocking.
- Institutional restraint: use violet for identity, focus, and selected actions rather than as a universal surface color.
- Trust native Albert: restructure only verified containers around native content without copying sensitive values or replacing official transaction controls.
- Tradeoffs: prefer page-family layout adapters operating on the original DOM over cloning or reconstructing Albert data. Keep unofficial assets, remote fonts, public-site imitation, and any data-reconstructed dashboard out of scope.

## Visual language

- Color: identity violet `#57068C`, black, white, pale violet, and quiet neutrals; semantic colors retain their conventional meanings.
- Typography: prefer NYU Perstare only when an authorized local font is available, followed by the documented system sans-serif stack.
- Spacing/layout rhythm: use the documented 4px-based scale and compact application density.
- Shape/radius/elevation: flat surfaces, 1px rules, 3px interactive-control radius, and no decorative shadow.
- Motion: 100–180ms functional feedback only, removed under reduced motion.
- Imagery/iconography: text-first interface with small geometric CSS indicators only; do not redraw the torch or substitute emoji.

## Components

- Existing components to reuse: native Albert links, tables, forms, buttons, alerts, and transaction flows remain the source of truth.
- New/changed components: `AppShell` becomes a fixed desktop application rail and compact mobile workspace header with extension identity, primary navigation, current-area context, page-family Quick access shortcuts, a concise featured NYU resources group, an on-demand task finder at every supported width, a skip-to-native-content action, native-view trust notice, and disable action. The desktop finder is a flat full-height workspace-covering surface beside the rail with a bounded content column; the compact finder expands inside the header. Both are Shadow-DOM-only, ephemeral modal navigation dialogs, use task-first labels, contain their Tab cycle, and delegate through the same verified callbacks as the primary rail, Quick access strip, and resource group. Escape and Close dismiss the general finder; in resource-only mode they also delegate to Albert’s original resource toggle so the student returns to the page in one step, while the separate “View Albert resource directory” action reveals the unchanged native directory. Their search field filters only the in-memory verified view model and stores nothing. Mirroring NYU's public directory pattern, the finder groups the full verified Other Resources allowlist under short student-facing category headings while the rail retains only four featured destinations. A sticky “Jump to” navigator keeps Search, Areas, Task shortcuts, every currently visible resource category, and Close reachable throughout long scrolling; desktop and ordinary 280–899px compact widths expose every jump in wrapped rows without horizontal overflow, while the ≤279px magnified layout retains a constrained horizontal scroller so the sticky index cannot consume the view, and the 44px-minimum Close control stays within the visible edge. Activating a jump scrolls to and focuses its heading without adding it to the Tab order. Search matches labels, descriptions, category names, and local non-persisted student-language keywords; natural phrases are normalized into meaningful words so filler such as “for” or “my” does not hide a verified destination, and the jump set contracts to the filtered result structure. While the finder is open, its host uses the browser's top stacking layer so non-modal native workspace content—including Albert's unusually high alert and promotional layers—cannot obscure the modal; closing restores the ordinary shell layer, and lifecycle detection still hides the host for real native dialogs so those remain authoritative. Any action invoked from the still-visible desktop rail closes the finder before delegating, so Albert's native Other Resources overlay can never open behind it. Resource discovery is constrained to direct anchors under the single exact native `#SUBMENU_ID_NYU_OTHER_RESOURCES_FLDR` boundary and preserves those anchors as the navigation authority. An `AdapterManager` selects exactly one primary structural adapter and owns atomic rollback. A `DomPatchJournal` is the sole native-DOM mutation surface and records only extension-owned attributes. Five observed selected-workspace adapters present `.isSSS_Wrp`, `.isSSS_Main.selected`, `.is_bb_LinkContainer`, `.is_bb_LinkColumn`, and `.is_bb_LinkItem` as distinct responsive workspaces. A generic PeopleSoft adapter frames verified page titles, direct groups, forms, tables, and grids without moving controls. The exact Class Search adapter creates a filter/results workspace. Other Resources stays a delegated native overlay, not a reconstructed page. A read-only modal enhancer handles only allowlisted titles, while any open native dialog temporarily suppresses the rail.
- Variants and states: Home, Academics, Grades & Transcripts, Finances, Personal Info, native Other Resources overlay delegation, generic Albert, tool-present/tool-unavailable, loading/reconciling, disabled, and failure rollback.
- Token/component ownership: `src/design-system/tokens.css` owns Shadow-DOM tokens; `src/design-system/native-theme.css` duplicates only the required prefixed values under the extension-owned root attribute because Shadow DOM custom properties do not cross into native documents.

## Accessibility

- Target standard: WCAG 2.1 AA.
- Keyboard/focus behavior: shell navigation and disablement are native buttons with visible focus rings and 44px minimum targets; native document order and controls are not intercepted. Activating the already-current area focuses the verified native workspace instead of delegating a redundant Albert navigation and reload. Opening the task finder by its button or the documented `/` shortcut moves focus to its search field, its result summary is announced politely, Clear remains keyboard reachable, and the dialog retains its contained Tab cycle and Escape-to-close behavior. The shortcut never intercepts `/` from a native editable control. Albert's original `#skiptocontent` link remains focusable and functional, but its wrapper uses the same off-canvas-until-focused pattern as the extension skip action so it cannot obscure task cards at compact heights. When the complete compact masthead is verified, its preserved native utility wrapper receives only a reversible `navigation` role and “Official Albert tools” accessible label; existing roles and labels win, and all extension-added semantics disappear on disablement or fail-open cleanup.
- Contrast/readability: violet, ink, white, and neutral combinations must meet AA for their text role.
- Screen-reader semantics: use a labelled banner, labelled navigation, `aria-current` for the current area, explicit status text, and explicit button text.
- Reduced motion and sensory considerations: remove nonessential transitions under `prefers-reduced-motion`; never rely on color alone.

## Responsive behavior

- Supported breakpoints/devices: single-column mobile below 600px, compact tablet workspace from 600–899px, desktop application rail from 900–1199px, and a bounded wide canvas at 1200px and above.
- Layout adaptations: at 900px and above use a fixed 264px scrollable application rail and offset the verified top-level native workspace without moving it; the rail separates current-area Quick access from universal NYU resources. Its 44px-minimum “Find a task” control opens a full-height, independently scrollable panel beside the rail, never wider than the remaining viewport, with task areas spanning two columns when space permits and Quick access beside NYU resources. Below 900px use a compact sticky top header and a complete three-column primary-area grid as the fast path, reducing to two columns only under 280px; all six original Albert areas remain simultaneously visible without horizontal scrolling, while current-area Quick access wraps into visible task rows. The same finder expands in place with separate What you can do, Quick access, and NYU resources sections. The compact Resources action uses a short visual label while retaining the complete accessible name, and becomes a full-row, high-contrast Close action while the native directory is open. Area cards are task-first inside the finder even though the compact fast-path controls retain Albert's native area labels. When the complete native masthead is verified, its original utility controls remain in a quiet 48px toolbar with 44px targets; do not repeat a system-oriented visible heading above the student’s actual workspace. The shell publishes one ephemeral, extension-owned height custom property so Albert's delegated Other Resources overlay occupies exactly the viewport remaining below the compact shell; it is restored on unmount and never persisted. At short heights the finder and native resource directory remain independently scrollable, and neither native masthead navigation nor unfocused native skip links may overlap their controls. Family and deep-page layouts reduce columns without document-level horizontal overflow. Below 900px all content follows source order in one column. Wide tables may scroll inside their marked region, but the document itself must not overflow horizontally.
- Zoom: at 200% zoom no shell control, native action, validation message, or page region may be clipped or overlapped. When magnification reduces the effective layout viewport below 280px, the already-labelled finder omits its redundant introductory block and reduces vertical padding so the focused search field is visible immediately rather than beginning below the viewport.
- Touch/hover differences: keep the disable target at least 44px high and never make hover the only feedback state. Hover may tint unselected area rows, but it must not override the current area's high-contrast selected surface.

## Interaction states

- Loading: do not show a blocking loader; start a lightweight lifecycle at `document_idle` and reconcile when PeopleSoft markers arrive.
- Empty/unknown: retain the generic “Albert” context and native page; do not invent an empty-state interpretation. A task search with no matches states that no verified destination matches and recommends a broader term; it never falls back to a guessed or remote result.
- Error: remove partial extension UI, root attributes, and injected styles; leave native Albert unchanged. Within a successfully adapted family page, native `role="alert"`, `.PSERROR`, and `.PSMESSAGE` surfaces retain Albert's exact foreground and background semantics; the extension may add responsive padding and a `currentColor` edge but never normalize validation, warning, or error messages into one shared surface.
- Success: show the page-aware application frame and one scoped structural adapter without hiding, cloning, or replacing native content. Native `role="status"` foreground and background semantics remain authoritative for account notices, completion states, and other live status messages.
- Disabled: the shell’s visible “Original Albert” escape hatch uses the accessible action name “Use original Albert” and explains that Better Albert stays off until the browser extension icon restores it. Activating it rolls back the adapter journal first and removes the shell, native theme, and every extension-owned attribute immediately while retaining only lifecycle listeners needed for re-enablement. The toolbar badge changes to OFF and its title explicitly offers “click to turn it on.”
- Offline/slow network: no extension network requests or remote assets are allowed.

## Content voice

- Tone: direct, calm, transparent, and concise.
- Terminology: use “Better Albert,” “unofficial local enhancement,” and explicit native-system language.
- Microcopy rules: describe actual state; do not imply official NYU endorsement or transaction success. Search language says “verified destination” and “available in this Albert view” so students understand both the utility and its boundary.

## Implementation constraints

- Framework/styling system: React and TypeScript built by Vite/CRXJS; Shadow DOM for extension UI; one extension-owned, root-prefixed stylesheet for native Albert presentation.
- Design-token constraints: tokens are prefixed `--ba-`; native-page values exist only inside selectors beginning with `html[data-better-albert-enabled]`.
- Performance constraints: one debounced document observer plus a related-context observer only for same-origin child/popup documents; no polling, remote assets, bulk text extraction, or background network activity.
- Compatibility constraints: Manifest V3 Chromium baseline; one explicit `sis.portal.nyu.edu` host permission plus a declarative `sis.nyu.edu` content-script match constrained to the proven Class Search/cart component path; `all_frames` is required for PeopleSoft child-document rendering, while top-level shell ownership remains unique.
- Safety constraints: authentication exclusions win; each adapter declares required selectors and applies only when all anchors match uniquely; optional sections may disappear without breaking the page; missing, ambiguous, detached, or throwing anchors roll back exactly to native presentation. Page-tool discovery requires exactly one connected, visible, enabled label match inside the allowlisted tool-container boundary; the same native node discovered through nested verified containers is deduplicated before uniqueness is evaluated. No document-wide text extraction or guessed selectors is allowed. Original nodes, IDs, names, values, event handlers, form ownership, DOM order, messages, and form input remain unchanged. Transaction-changing labels are excluded from extension navigation. Semantic warning, error, success, financial, and transaction-control colors are not overwritten. The shell may publish the ephemeral `data-better-albert-task-finder-open` presentation marker on its current and delegated-control documents only while modal search is open; it removes the marker on close, unmount, and failed mount, and uses it solely to prevent duplicate native-directory interaction behind resource search.
- Test/screenshot expectations: sanitized fixtures cover all five observed selected workspaces plus native Other Resources delegation and universal resource discovery, realistic deep forms, read-only and unknown transaction modals, Class Search success/empty/error states, authentication, unknown surfaces, and partial failure. Unit and lifecycle tests prove that resource and page-tool shortcuts require unique exact-label native controls, fail open for missing/duplicate/nested/disabled/hidden controls, update after PeopleSoft replacement, and delegate to the original anchor. Browser tests prove task-first cross-family direct-tool discovery and native activation from Home; finder search focus, matching across areas/tools/resources, result counts, clearing, reset-on-close, no-result messaging, filtered native activation, sticky jump visibility after long scrolling, focused section jumps, filtered jump contraction, persistent close access, and finder stacking above ordinary native workspace layers with restoration after close; resource availability across every selected workspace; layout regions at 400, 768, 900, 1200, and 1440px; task-first area names and combined accessible names at every supported width; desktop finder containment beside the rail at 900, 1200, and 1440px; the compact finder at 400, 768, and 899px; grouped task areas and NYU resources; keyboard open/Escape/focus return and Tab-cycle containment; rail action closure before native Other Resources overlay entry; native delegation; 400×376 short-height scrolling without an unfocused native skip-link collision; native skip-link focus and activation; document overflow; 200% zoom; keyboard focus; disabled rollback; DOM replacement; missing selectors; native control identity/form ownership/click behavior; storage/network privacy; and exact Class Search host containment.

## Page layout contracts

| Surface | Required full-page layout | Safety boundary |
| --- | --- | --- |
| Home | Full-width page context; updates/deadlines, schedule/today, enrollment-date/action, and native tool-directory regions; desktop primary workspace plus supporting column; mobile source order. When an Albert release omits a native heading or leaves an accessible heading reference unresolved, the updates carousel, true schedule region, or account region receives a reversible orientation cue only; native headings always win. The exact native Course Search destination appears as the task-first “Find Classes” one-step shortcut; when a student starts from another verified workspace without a direct Course Search control, that same action delegates to Home and opens the original Course Search once Home is ready. The original To Do, Holds, and Enrollment Dates panels form one responsive equal-card status grid: three bordered cards in the ordinary desktop attention column; two cards plus an odd full-width final card at 401–599px; and one column at 400px and narrower. The extension supplies only grid geometry, spacing, borders, and focus treatment; each card retains Albert’s native foreground and background urgency semantics, so warnings, blocking holds, and informational enrollment dates must not be normalized to a shared surface color. Their headings, status roles, values, and source order remain native. When every compact-masthead anchor verifies uniquely, Albert’s preserved utility controls form a quiet 48px official toolbar with 44px targets instead of floating raw above the workspace. The wrapper retains the reversible “Official Albert tools” accessible navigation label, but the visible duplicate heading is omitted so the student reaches Home content sooner; native link labels remain unchanged and all added attributes roll back. Native enrollment actions receive the same minimum geometry without semantic recoloring. | Do not clone appointments, holds, schedule entries, counts, or enrollment values. Preserve native utility and enrollment labels, destinations, listeners, and behavior. Missing compact-header or overview anchors fail open to the complete native masthead or common hub layout. |
| Academics | Page context; task-first Quick access before the broad area directory; distinct planning, enrollment, degree-progress, and graduation boards; deep pages use the common title/group/form/table frame. Because routine descriptions disappear in the compact shortcut strip, extension-owned labels lead with the student job—Plan Future Courses, Find My Advisor, Schedule Advisor Meeting, Check Degree Progress, Explore Another Program, and Check Graduation Status—while descriptions retain Albert terms such as Academic Planner and What If Report. Find My Advisor focuses the unchanged uniquely verified advising section; the other actions delegate to unchanged native controls. Only when the adapter verifies exactly one native Planning, Degree, Advising, Enrollment, and Graduation section, those unchanged sections become a five-stage academic journey—Plan your path, Check requirements, Meet your advisor, Review enrollment, and Track completion—with matching visible overlines and reversible labelled-region semantics. Source order remains authoritative; desktop keeps the first two stages paired, advising full-width, and enrollment/completion paired, while mobile remains one sequential column. Incomplete or ambiguous section sets receive no journey labels. A verified form with exactly one submit labeled Enroll becomes a clearly labeled “Official Albert enrollment step” with the review-before-submit note “Review your selections in Albert before submitting.” That exact note is mirrored into a reversible `aria-description` only when the native form has neither `aria-description` nor `aria-describedby`; native Albert help and validation relationships always win. Its native control retains dependable 44px-minimum touch geometry. | Applications, submissions, add/drop/swap, and enrollment actions remain native and are never promoted into synthetic controls; preserve their labels, values, form ownership, listeners, semantic styling, validation, and source order. Do not label ambiguous journeys or enrollment forms, and restore every added region, label, and boundary on rollback. |
| Grades & Transcripts | Page context; an explicit records guide distinguishes viewing grades from the transcript and enrollment-record options currently verified in Quick access; a numbered, compact academic-career and term filter that joins into one panel on phones while preserving source order; native academic-record table workspace; separate records/report directory. The guide is mirrored into a reversible `aria-description` only when its native host has neither `aria-description` nor `aria-describedby`; native Albert help always wins. View Grades is the first Quick access action only when the adapter verifies one enabled native Academic Career selector or chooser inside one exact career-selection region; it focuses that original selector or chooser without changing its value or tab order so grade viewing leads the page instead of enrollment proof. Missing, disabled, or ambiguous selectors or choosers produce no shortcut. Because routine descriptions disappear in the compact shortcut strip, extension-owned labels lead with the student job—View Test Scores, Get Unofficial Transcript, and Review Transfer Credit. “Proof of Enrollment” delegates to the native National Student Clearinghouse destination, while “Share Enrollment in MyHub” names the distinct MyHub workflow in student language. Both keep their provider descriptions and remain one-step Quick access actions. When the exact University Registrar resource is uniquely verified, “Official Transcript Guidance” appears alongside them, delegates to that unchanged original anchor, and keeps the explanation “Open NYU Registrar instructions for ordering an official transcript” visible even on phones; missing or ambiguous Registrar evidence omits the shortcut. Native selectors and choosers retain 44px-minimum touch geometry. | Preserve official grade strings, selector values, chooser labels, and table semantics exactly. Never imply that opening Registrar guidance orders or submits a transcript. Add guidance only when the records directory is a complete verified Quick access mirror, restore it on rollback, and never calculate GPA, copy grades into cards, store values, or merge the two enrollment-verification destinations. The View Grades shortcut may add only reversible region/focus markers to the native selector or chooser and must not select a career or term on the student’s behalf. |
| Finances | Page context; separate account, statements, and financial-aid regions; clear native boundary for external payment systems. Because Bursar and eSuite are provider/system terms rather than student jobs, extension-owned shortcuts lead with the outcome—Check Account Balance, Pay Tuition & View Bills, Get Account Statement, and Check Financial Aid Status—while their attached descriptions retain the native Bursar/eSuite/account terminology. Verified balance, eSuite, and statement destinations remain one-step Quick access actions. Check Financial Aid Status delegates to the exact native status control when it exists; otherwise, only when the adapted page contains one uniquely verified native financial-aid region, the same shortcut focuses and centers that unchanged section so aid does not disappear below payment content. Duplicate or missing aid regions produce no fallback shortcut, and a verified direct native status control always wins. When the exact nested bursar-link boundary contains only uniquely verified Quick access mirrors, it remains connected as the delegation authority but becomes inert and visually collapsed; an unknown or ambiguous action keeps the complete native boundary visible. Only when one account section contains exactly one form with exactly one “Make a Payment” submit control, that same form receives the reversible accessible name and visible overline “Official Albert payment step,” plus the explanation “Payment processing remains outside Better Albert.” The exact explanation is also mirrored into a reversible `aria-description` only when the native form has neither `aria-description` nor `aria-describedby`; native Albert help always wins. Ambiguous or missing payment forms receive no boundary label. Native payment submit controls receive only dependable 44px-minimum touch geometry and spacing. | Never copy or persist balances or awards. Payment and award actions retain their native label, URL, node identity, form ownership, values, semantic styling, listeners, and behavior; the extension never supplies values, submits on the student’s behalf, intercepts payment, or implies that a transaction succeeded. A section-focus fallback may add only reversible focusability and must not promote Accept/Decline Awards or any other transaction. |
| Personal Info | Page context explicitly distinguishes reviewing official details from updating contact information; four reversible visual group cues—Official details, Contact information, Safety contact, and Official records—divide the long native page without moving sections or replacing their headings; distinct profile/contact summary and native edit-form workspaces; aligned labels, controls, and errors. A verified native form with one Save and one Cancel action becomes a clearly labeled, full-width “Official Albert edit form” boundary with an explicit privacy note—“Albert saves these changes. Better Albert does not store this information.”—including a one-column phone layout. The same sentence is exposed through a reversible `aria-description` only when the native form has neither `aria-description` nor `aria-describedby`; native Albert help and validation relationships always win. Verified demographic, address, phone, email, and emergency-contact destinations remain one-step Quick access actions. Natural student requests such as “new address,” “new phone number,” “new email address,” and “new emergency contact” resolve directly to the corresponding exact native edit link when it is uniquely verified instead of stopping at the broader Personal Info area. Because routine descriptions are hidden in the compact shortcut strip, each extension-owned compact label names the student action directly—Review Personal Details or Update Addresses, Phone Numbers, Email Addresses, and Emergency Contacts—while descriptions retain Albert terminology and delegation still activates the unchanged native control. A uniquely verified native “Indicate My Pronouns” link may appear as the task-finder-only “Update Pronouns” destination; it delegates to that original control without copying identity values or implying a new form. The longer read-only Missing person contact, Citizenship, and Identification sections remain out of the compact shortcut strip, but each becomes a precise task-finder-only destination when exactly one corresponding native section is verified; activation focuses and centers that unchanged section without copying its values or implying an edit action. “Preferred name” may resolve to the verified read-only personal-details destination, but the extension must not imply that the student can edit it. Native edit links and form actions receive 44px-minimum touch geometry without semantic recoloring. | Never clone, cache, log, or expose identity/contact values. Preserve native labels, descriptions, form identity and ownership, values, autocomplete, validation, listeners, and save/cancel behavior. Do not label ambiguous forms or forms without the exact Save/Cancel boundary, and restore all added attributes on rollback. |
| Other Resources | This observed surface is a native overlay, not a selected page document. The shell delegates to Albert's original trigger and, only under its single exact stable submenu boundary, presents the original 21 direct anchors as a titled, category-cued directory with a persistent compact Close action. The rail may delegate to four featured direct anchors, while the task finder may group and search all 21. Any distinct resource document still receives the generic/deep adapter. | Use only uniquely matched original native destinations; preserve link identity, labels, listeners, and URLs; add no tracking parameters, remote metadata, copied URLs, or synthetic links. Missing or ambiguous anchors are omitted and the native overlay fails open. |
| Deep PeopleSoft pages | Bounded workspace with page title, native breadcrumbs/back control, direct grouped sections, readable labels, aligned fields, inline validation, native tables, and native action area. Classic deep components use the unique `.ps_box-page` boundary; authenticated utility pages with a verified PeopleSoft response heading may use the unique `#IS_AC_RESPONSE > .ptprtlcontainer` response wrapper. Visible breadcrumb links, selectors, and native actions receive 44px-minimum touch geometry across phone and desktop layouts. When a top-level same-origin deep window retains its already-trusted Albert opener, the shell reuses that opener’s verified original area, task, and Other Resources controls so Find a task and cross-area wayfinding do not disappear. Because the current family label on a detail page identifies its parent area rather than the document already in view, activating that current-area control must delegate back to the opener’s exact native area control and focus the authoritative Albert window; only a true selected family workspace treats its current-area control as an in-page content skip. When the adapted deep page exposes exactly one non-empty page title and that title exactly matches the native label of exactly one verified opener tool, omit that one tool from Quick access and task search because it points back to the page already in view; ambiguous, duplicate, or missing title matches suppress nothing. Delegation activates every remaining original opener control and returns focus to that authoritative Albert window; it never copies a destination into the deep document. | Unknown transactional forms receive framing and geometry only. Do not change labels, semantic colors, control order, form ancestry, values, listeners, validation, or status colors. Related controls are used only when the current deep document has no verified primary area navigation, the opener remains connected, same-origin, on the allowlisted portal host, and non-authentication; otherwise the deep page fails back to its own controls or unavailable states. |
| Read-only modals | Viewport-centered surface, violet title band, bounded content, 44px native actions, responsive sizing, and a fixed mask. | Only allowlisted read-only titles are enhanced; native focus behavior and unknown/transaction dialogs fail open. |
| Class Search | A uniquely verified combined subject/catalog number/title/instructor field becomes a task-first “Find a class” block that explicitly says one field accepts all four criteria, uses a matching non-prescriptive placeholder and dependable accessible name, and ends with one full-width native Search action. The exact visible helper is mirrored into a reversible `aria-description` only when the control has neither a native `aria-description` nor `aria-describedby`; native help and validation relationships always win. A separately verified subject-only field instead says “Search by subject” and “Enter a subject”; it must never promise title, instructor, catalog-number, or universal keyword matching. On the exact classic selector-only page, the unique Academic Career and Subject selects form a “Choose a subject” path with 48px full-width controls and the one exact native Search action as its full-width primary button; ambiguous or incomplete selector sets receive no such promotion. Unrecognized or ambiguous text fields receive no task-first promotion. On the legacy combined page, the slower Course Description query and native advanced criteria remain available but appear as a quiet “More search options” section with full-width text/action geometry, 20px checkbox/radio controls inside 44px rows, and a contained native file control, so they do not compete with the one-field path. Fluid criteria render as a clear label/control sequence with full-width 48px inputs and selects instead of browser-default inline fragments. Result rows become scannable course cards, but each native availability state—including Open, Closed, and Waitlist—must retain its original text, foreground, and background semantics; the extension may add geometry and a `currentColor` border but never normalize different outcomes into one shared color. Every proven variant uses a 280–320px desktop filter column plus flexible native results workspace; mobile stacks filters then results in source order. Every visible native button retains a minimum 44px touch target. When the result-transaction group is uniquely verified after results and contains only the exact native Add to Cart, Enroll, or Continue actions, it becomes a clearly bounded row with the visible overline and reversible accessible name “Official Albert next step”; an existing native `aria-label` or `aria-labelledby` always wins. The row follows results rather than the taller desktop filter, and only the uniquely verified primary Search receives the violet primary treatment. Title wrappers do not become redundant empty cards. PeopleSoft’s first/last skip sentinels remain in native focus order and keep their original targets, but use the same off-canvas-until-focused pattern as other native skip navigation. | Search, Add to Cart, Enroll, Continue, details, and skip navigation remain original controls with original listeners and form ownership. The extension neither supplies criteria nor submits searches, and adds no API bridge or enrollment automation. |

## Adapter readiness and fail-open

- Each adapter has a documented required-selector set and fixture contract.
- Preparation gathers DOM references and non-sensitive layout enums only; it never copies page values.
- Application writes extension-owned `data-better-albert-*` attributes, one reversible `--ba-native-shell-height` presentation property on the document root, and, only when absent, a journaled `tabindex="-1"` skip target. A uniquely proven combined or subject Class Search field may also receive a reversible task-first `aria-label`, non-prescriptive placeholder, or matching `aria-description` when the corresponding native guidance is absent; native `aria-describedby` help and validation relationships take precedence. All changes use one rollback journal. The extension never uses `innerHTML`, clones native nodes, or moves native controls.
- Every major visible native section is assigned a documented layout region or explicitly left untouched as a safety exception.
- A primary adapter exception rolls back that adapter without poisoning future lifecycle reconciliation; shell failure removes all extension presentation.
- Reconciliation is idempotent and treats detached anchors after PeopleSoft partial replacement as stale.

## Open questions

- [x] Confirm the public launcher and authenticated application host boundary from sanitized browser evidence: `albert.nyu.edu` launches authentication, while the Albert application loads at `sis.portal.nyu.edu`.
- [x] Confirm primary authenticated navigation labels: Home, Academics, Grades & Transcripts, Finances, Personal Info, and Other Resources.
- [x] Confirm that the v0.1.0 Shadow-DOM header mounts on the authenticated `/psp/` shell and that a header-only release is visually insufficient.
- [x] Confirm shared hub-board markers from the authenticated shell: `.is_bb_LinkContainer`, `.is_bb_LinkColumn`, and `.is_bb_LinkItem`.
- [x] Confirm allowlisted read-only tool labels for all six primary navigation contexts without retaining page values.
- [x] Confirm PeopleSoft lightbox markers on a read-only degree-progress route: `body.iLightboxOpen`, `.ps_modalmask_cover`, `#pt_modals.PSMODAL`, `.ptpopuptitlebar`, `.PTPOPUP_HEADER`, and `.PTPOPUP_INNER`.
- [x] Confirm that Class Search/cart renders in a cross-origin iframe at the exact `sis.nyu.edu` component `NYU_SR_FL.NYU_SSENRL_CART_FL.GBL` and requires a second narrow manifest match.
- [ ] Capture version-stable PeopleSoft selectors for each deep transactional form; until proven, transaction forms retain native layout and semantic colors.
- [ ] Confirm which live routes render in same-origin `/psc/` child documents across Albert versions.
- [ ] Confirm the supported Chromium/browser version matrix.
- [ ] Determine whether authorized NYU font and wordmark assets can be distributed; continue using text and local fallbacks until then.

# Evidence Appendix: NYU Website Design Language

## Scope and Fidelity

This guide records the current [NYU homepage](https://www.nyu.edu/) as inspected on July 15, 2026 at 400px, 880px, and 1435px viewport widths. It covers the public NYU website only and is a build specification, not a replacement for official brand standards.

A visually exact implementation requires the authorized NYU Perstare webfont files, official torch/wordmark and icon assets, the same photography and crops, identical copy, and screenshot comparison at every breakpoint. Without those inputs, use this guide for close structural fidelity but do not claim a pixel-identical result.

## Core Character

NYU’s public website feels bold, urban, editorial, and direct. It combines NYU Violet with black-and-white layouts, oversized typography, immersive photography, flat geometry, and generous whitespace. The design avoids ornamental cards, soft shadows, and excessive rounding. Hierarchy comes from scale, contrast, alignment, and image cropping.

## Color System

| Role | Observed token | Usage |
| --- | --- | --- |
| NYU identity | `#57068C` | Navigation, primary actions, links, labels, and brand accents |
| Primary ink | `#0B0B0B` | Display headlines, body text, and black feature sections |
| White | `#FFFFFF` | Primary canvas, reversed text, and breathing space |
| Campaign violet | `#8900E1` | Occasional full-width promotional surfaces |
| Pale violet | `#EEE6F3` | Quiet branded surfaces and selected states |
| Neutral surface | `#F7F7F7` | Tab areas and low-emphasis sections |
| Muted ink | `#5C5C5C` | Captions, legal text, and metadata |
| Rule | `#E4E4E4` | Section dividers and list separators |

Use campaign violet as a solid editorial interruption, not as the default action color. Semantic success, warning, and error colors must retain their meaning and should not be replaced by violet.

## Typography

Use `"NYU Perstare", NYUPerstare, "Helvetica Neue", Helvetica, Cantarell, Ubuntu, Roboto, Noto, Arial, sans-serif`. NYU Perstare is fidelity-critical; a fallback changes character widths, wrapping, and vertical rhythm.

| Role | Size / line-height | Weight | Tracking |
| --- | --- | --- | --- |
| Body | `16px / 24px` | `400` | normal |
| Section heading | `28px / 33.6px` | `700` | `-0.42px` |
| Hero, handset | `40px / 34px` | `900` | `-1.6px` |
| Hero, 769–1199px | `48px / 40.8px` | `900` | `-1.92px` |
| Hero, 1200px+ | `84px / 71.4px` | `900` | `-3.36px` |

Keep the hero all caps and extremely compact at `0.85` line-height with `-0.04em` tracking. Use sentence case for section headings, short violet eyebrow labels where shown, and approximately 45–75 characters per line for long copy.

## Layout and Spacing

Base spacing on `4, 8, 12, 16, 20, 24, 28, 32, 48, 64, 80, 96px`. Use 20px horizontal gutters on handsets, 24px on medium layouts, and 32px on desktop.

- At 900px and above, reserve a fixed 92px violet rail. Content begins 32px after the rail and ends 32px before the right edge.
- Let the hero image span the full content width. Use `object-fit: cover`; change the crop by breakpoint instead of stretching the image.
- Stack editorial modules in one column on handsets. Restore image/text pairs, news columns, vertical dividers, and alternating image placement as width allows.
- Use 64–120px between major sections and 1px rules between related news items.
- Keep panels flat: use no card shadow and only a 3px radius on interactive controls.
- Use black, identity violet, or campaign violet bands sparingly to reset long-page rhythm.

## Responsive Behavior

The live styles include meaningful tiers at 600px, 768/769px, 900px, 1200px, and 1585px. Treat 900px as the navigation switch and 1200px as the wide-display typography switch.

- **Below 900px:** show a roughly 58px full-width violet header with hamburger/close at left, compact NYU wordmark, and search at right. Opening navigation fills the viewport with violet, locks body scrolling, lists five primary destinations with right chevrons, and pins Give, All NYU, Info For, and Log In actions at the bottom.
- **900px and above:** replace the mobile header with the fixed 92px rail. Put the official torch mark at top, five icon/label destinations down the rail, and All NYU at bottom.
- **1200px and above:** enlarge the hero title to the 84px display token and retain 32px content gutters.
- **Content reflow:** preserve document order. Images, headlines, summaries, and linked lists become a single readable column rather than shrinking desktop grids.

## Navigation and Interaction

- The desktop rail pairs simple line icons with short labels. The NYU torch mark sits at the top and “All NYU” is isolated at the bottom.
- Utility actions sit over the hero in the upper-right. Controls are rectangular and tightly grouped.
- Audience navigation opens as a solid violet dropdown with white text and no decorative container chrome.
- Search becomes a near-full-screen black overlay with a violet search field, search icon, and explicit close control.
- Use obvious selected states. The homepage’s vertical tabs use violet labels and a flat light-gray active row.
- Use the official NYU SVG marks and the same thin-stroke icon family. Do not redraw the torch, substitute emoji, or mix filled and outlined icon styles.

## Motion and Animation

Motion is functional rather than ambient. During the desktop inspection, the hero and campaign imagery remained static, ordinary scrolling did not introduce parallax or scroll-jacking, and the fixed navigation rail provided visual continuity. Content remained understandable without waiting for a reveal or animation.

### Observed Interaction Patterns

- **Audience menu:** flip the trigger chevron and reveal the solid violet list directly beneath it. Use a compact fade or slight vertical reveal; do not bounce or float the menu.
- **Search:** present the violet field, fade a black veil over the page, and move focus into the input. Closing should reverse the state clearly and return the page without a layout jump.
- **Story tabs:** update the active gray row immediately and replace the related image and copy in place. Avoid horizontal carousel travel, height animation, or movement that displaces the user’s focus.
- **Scrolling and media:** keep native scrolling and static editorial compositions. Do not autoplay the hero, campaign media, or decorative loops.
- **Links and buttons:** limit feedback to color, underline, border, opacity, or a small chevron shift. Avoid hover lift, elastic easing, and decorative bounce.

### Recommended Motion Tokens

These timings are implementation targets, not measurements of NYU’s source CSS.

| Pattern | Target duration | Treatment |
| --- | --- | --- |
| Hover or focus feedback | `100–180ms` | Color, border, underline, or opacity |
| Menu disclosure | `150–250ms` | Fade plus no more than `4px` vertical movement |
| Tab content change | `120–200ms` | Direct swap or subtle opacity crossfade |
| Search veil | `200–400ms` | Opacity with restrained ease-out |

Use standard `ease-out` for entrances and `ease-in-out` for reversible color or opacity changes. Under `prefers-reduced-motion: reduce`, remove transforms, parallax, auto-advance, and nonessential fades; preserve immediate state changes, focus movement, and visibility. Any necessary video must expose play/pause controls and must not start with sound.

## Components

### Buttons and Links

- Primary button: identity-violet fill, white `14px / 21px` text, `12px 16px` padding, 3px corners, and no shadow. The inspected button was 47px high.
- Secondary button: white fill, one-pixel violet border, violet text.
- Editorial links: bold black or violet text with a simple right arrow/chevron.
- Group related primary and secondary calls to action side by side.
- Provide visible keyboard focus and at least a 44-by-44-pixel interactive target where layout permits.

### Editorial Modules

- Use borderless image-and-copy compositions rather than floating cards.
- Crop images decisively and preserve rectangular edges.
- News lists use thin separators, bold titles, and chevrons.
- Feature sections pair one dominant image with a large headline, concise paragraph, and single action.
- Avoid gradients, badges, and pills unless they communicate a specific state or campaign identity.

### Footer

- Separate the footer with a thin gray rule.
- Use multiple aligned columns: muted legal copy, violet utility links, campus links, and social icons.
- A very pale oversized NYU wordmark may be used as background texture without reducing readability.

## Imagery

Choose candid, place-specific photography that communicates campus life, people at work, and NYU’s cities. Favor natural light, documentary framing, and strong environmental context. Preserve the source aspect ratio and focal point at each breakpoint; a different crop is visibly a different design. Use meaningful alternative text; decorative images should be hidden from assistive technology.

## New-student resource vocabulary

- StudentLink discovery follows the same safety boundary: “studentlink,” “student link,” and “StudentLink Center” may route to the verified Other Resources area when Albert does not expose an exact native StudentLink anchor, but the result must remain labeled as Albert’s services directory. The extension never claims that directory is StudentLink, copies a public StudentLink URL, or infers a bill, aid, registration, or international-services workflow.
- NYU’s “Student Guides” and “Resources for Students” headings are conservative aliases for the same verified Other Resources area when Albert exposes no exact guide anchor. The extension does not copy public guide URLs, imply that a guide is available in Albert, or turn the alias into a synthetic destination.
- NYU’s “Key Links,” “Student Information and Resources,” “Academic and Campus Resources,” “Academic Services,” “Getting Around Campus,” “Wellbeing,” “Wellbeing Resources,” “Get Support,” “Communities and Groups,” “How We Engage,” and “Housing and Dining” headings follow the same directory-only boundary. They may help a newcomer find the verified Other Resources area, but they never become a copied public-site destination or a promise that Albert contains a matching service.

- International-student discovery must not require prior knowledge of the OGS acronym. When Albert exposes one exact native OGS anchor, the resource finder includes a visible “International students” popular-resource starter and resolves ordinary requests for an I-20, DS-2019, F-1 or J-1 status, visa or immigration documents, pre-arrival or arrival check-in, travel signatures, I-94 or SEVIS guidance, CPT, OPT, work authorization, SSN, and international-student employment to that original anchor. This follows NYU’s current Student Visa & Immigration hierarchy across Newly Admitted Students, Current Students, Travel & Visitors, and Employment & Tax. The extension never collects immigration or identity data, assesses status or work eligibility, interprets a document, supplies legal advice, submits an OGS form, or reconstructs a deeper destination; Enter delegates only when the original OGS anchor is uniquely verified. Study-away and study-abroad intent remains distinct and continues to use the verified Office of Global Programs anchor.
- Student-success discovery must use NYU’s current service vocabulary rather than the outdated assumption that NYU Connect is a clubs directory. When one exact native NYU Connect anchor is verified, the ordinary “student success” query, its “Student success” popular-resource starter, and searches for a student success specialist, Success Network, individualized support, academic coaching, requesting assistance, or scheduling a support appointment delegate to that original anchor. Student Services must not compete for the generic “student success” phrase. Club, organization, event, activity, and getting-involved intent instead delegates to the exact verified Student Life anchor. The extension does not expose private success-network data, infer whether access has been granted, schedule an appointment itself, request assistance, promise availability, or construct a deeper NYU Connect destination.
- Current NYU Students service labels remain conservative aliases for the same verified Albert resources: “Tutoring and Help with Classes” opens Academic Support; “Accessibility and Accommodations,” “Athletics and Fitness,” “Sustainability,” “Student Centers and Spaces,” “Student Tech Guide,” “Gyms and Campus Recreation,” and “Student Tech Centers” open Campus Resources; “On Campus Living,” “Off Campus Living,” “Summer Housing,” “Dining on Campus and Meal Plans,” and “Basic Needs Assistance” open Housing; “Wellness Exchange,” “Counseling Services,” and “Health Education” open Wellness Center; “Career Development and Jobs,” “Find a Job or Internship,” “Connect with Alumni,” and “Entrepreneurship Resources” open Wasserman; and “NYU Engage,” “Student Government,” “Intramural and Club Sports,” “Service Opportunities and Civic Engagement,” “Multicultural Education and Programs,” “Veteran Services,” and “Center for Global Spiritual Life” open Student Life. These aliases delegate only to the exact original Albert anchor, never copy a public NYU URL, and never imply that the public-site subservice exists inside Albert.
- Student-ID discovery must not require a new student to know the “NYU Card Center” name. When one exact native NYU Card Center anchor is verified, requests such as “student ID,” “student ID card,” “school ID,” “lost ID card,” and “replace my card” resolve to that original resource. The extension does not create a card request, report a loss, expose card values, or invent a replacement workflow.
- New-student discovery is a visible Common Tasks starter, “New student help,” whenever Albert exposes its verified Other Resources area. Activating it searches the same safe “new student” intent and opens that original directory; equivalent newcomer wording such as “first-year student,” “newly admitted student,” “welcome week,” “first semester advice,” “Advice for Your First Semester,” “time management,” “transfer student,” “Advice for Transfer Students,” and “student tech guide” remains a conservative alias for that same area. These phrases mirror current NYU Students guidance vocabulary without adding a public-site destination. The extension does not invent an orientation destination or imply that a registration step is complete.
- Home’s workspace context uses the student-facing copy “Your starting point for classes, first-week help, and time-sensitive tasks” so a newcomer can understand why Home matters before learning Albert’s area names. This is orientation copy only; the underlying native Home content and status meanings remain unchanged.
- Orientation discovery must remain useful even when Albert’s current Other Resources menu has no exact Orientation anchor. Requests such as “orientation,” “new student orientation,” and “first year orientation” may route to the verified Other Resources area so the student can inspect Albert’s real directory; the extension must not invent an orientation URL or imply that registration is complete.
- Campus-mobility discovery may route “parking” to the verified Other Resources area when no exact native parking anchor is available. The extension must not invent a permit URL, parking availability, or a transportation transaction.
- Campus-service discovery may route “disability services,” “accessibility,” “bookstore,” “campus map,” “transit,” and “shuttle” to the verified Other Resources area when the current native directory has no exact Campus Resources anchor. If Albert exposes a uniquely verified Housing resource, “roommate” and “roommate matching” resolve to that original Housing destination. The extension never invents a service URL, accommodation decision, permit, purchase, or transit action.

## Fidelity Verification

Capture and compare full-page screenshots at 400, 768, 899, 900, 1199, 1200, 1435, and 1585px. Check the closed and open mobile menu, audience dropdown, search overlay, every tab state, hover, keyboard focus, and reduced-motion mode. Confirm that NYU Perstare has loaded before comparing, that headings wrap on the same words, that image focal points match, and that no horizontal overflow exists. Pixel comparison should exclude changing editorial content only when the source page has been updated.

## Quick Review Checklist

- Is violet used deliberately rather than everywhere?
- Are headlines bold, short, and clearly separated from body copy?
- Are surfaces flat, square, and free of unnecessary shadows?
- Does imagery carry the visual emphasis?
- Do the 900px navigation and 1200px display-type switches match the live behavior?
- Are buttons 3px-radius controls, either filled violet or cleanly outlined?
- Are focus, contrast, alt text, reduced motion, and semantic structure preserved?
